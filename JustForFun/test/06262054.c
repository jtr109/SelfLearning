#include <stdio.h>

int ivalidDigits(int num[], int digits) {
    if (num[0] == 0) return 1;
    for (int i = 0;i < digits; ++i) {
        for (int j = i + 1; j < digits; ++j) {
            if (num[i] = num[j])
                return 1;
        }
    }
}

int createDigits(int digits) {
    int num[];
    for(int i = 0; i < digits; ++i) {
        num[i] = 
    }
}

int main (void) 
{
    
}
