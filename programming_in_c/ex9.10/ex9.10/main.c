#include <stdio.h>

// test merge sort
void mergeSort (int numbers[], const int numberOfElement)
{
    int mid;
    
    if (count < 2)
        return;
    else {
        mid = numberOfElement / 2;
        
    }
    
    int L[] = {0};
}